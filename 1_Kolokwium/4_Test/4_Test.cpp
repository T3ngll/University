#include<iostream>
using namespace std;
int main()
{
	/*int arr[50];
	int D, G;
	cout << "Write elements(0 is finishing element):";

	for (int i = 0; i < 50; i++)
	{
		cin >> arr[i];
		if (arr[i] == 0)
		{
			break;
		}
	}
	cout << "Elements: ";
	for (int i = 0; arr[i] != 0; i++)
	{
		cout << arr[i] << " ";
	}
	cout << endl << "Write D: ";
	cin >> D;
	cout << "Write G: ";
	cin >> G;
	for (int i = 0;arr[i] != 0 && arr[i+1]!= 0 && arr[i+2]!= 0; i++)
	{
		if (arr[i] % D == 0 && arr[i+1] % D == 0 && arr[i + 2] % D == 0 && (arr[i] + arr[i+1] + arr[i + 2]) / 3 >= G)
		{
			cout << arr[i] << " " << arr[i+1] << " " << arr[i + 2] << "   ";
		}

	}*/

}